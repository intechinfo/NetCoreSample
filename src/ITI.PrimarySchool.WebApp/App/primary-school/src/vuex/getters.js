// App
export const isLoading = ({ app }) => app.isLoading // eq. export const isLoading = state => state.app.isLoading

// Classes
export const classList = ({ classes }) => classes.classList

// Students
export const studentList = ({ students }) => students.studentList

// Teachers
export const teacherList = ({ teachers }) => teachers.teacherList
